export type Facility = {
  id: string;
  name: string;
  timeFormat?: string;
  dateFormat?: string;
  dateTimeFormat?: string;
  timeZone: string;
};

export type Organisation = {
  id: string;
  name: string;
};
